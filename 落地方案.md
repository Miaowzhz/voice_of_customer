# 厨具 VOC 洞察与闭环 Agent：MVP 落地方案

## 目标

把“人工整理用户反馈、判断问题、通知责任人、追踪处理”压缩成一个可重复执行的流程。第一版只做分析与协同，不自动改价、退款或修改商品页面。

## 已确认边界

- 交付目标：面试作品 Demo。
- 数据入口：Excel/CSV，字段至少包括 `feedback_id`、`text`、`sku`、`channel`、`created_at`、`order_id`（可选）。
- 飞书：可以创建应用并调用多维表读写、消息推送 API。
- 使用对象：客服主管、品控、商品、供应链和直播运营。
- 输出：飞书多维表 + 飞书群/私聊通知 + 周报。
- 运行方式：n8n 编排，Python 负责清洗聚合，LLM 负责结构化理解和建议生成。Dify/Coze 不作为必需依赖，避免面试 Demo 同时维护两套编排。
- 数据规模：首版每批 500～5000 条反馈。

## 面试 Demo 的最终形态

准备一份脱敏或模拟的 `feedback_demo.xlsx`，面试现场通过 n8n Webhook 或表单上传，点击一次运行。3～5 分钟内完成：

1. 解析并清洗反馈；
2. 输出 10 类厨具问题的统计和趋势；
3. 生成 Top 5 问题、原文证据和建议动作；
4. 写入飞书 Feedback 与 Issue 两张表；
5. 向飞书群发送“新增高优问题”卡片；
6. 修改 Issue 状态后再次运行周报，展示问题闭环和下一步建议。

演示数据要人为植入一个明显事件：例如某 SKU 在最近 7 天“涂层粘锅”反馈突然升高。这样可以清楚展示 Agent 从反馈发现异常、定位商品、分派品控任务到复盘趋势的全过程。

## 推荐技术实现

- **n8n：**Webhook/File Trigger、HTTP Request、条件分支、重试、飞书通知和定时周报。
- **Python 3.11：**`pandas` 清洗、文本去重、SKU 归一、聚合统计；用 `pydantic` 校验模型 JSON。
- **LLM：**选一个可稳定调用的 OpenAI 兼容模型，使用结构化输出；模型供应商不影响工作流设计。
- **飞书开放平台：**多维表写入、记录更新、群机器人卡片消息；App Secret 放在 n8n 凭据中，不写入代码仓库。
- **持久化：**Demo 阶段用本地 SQLite 或 n8n execution data 保存运行批次和幂等键；不引入数据库集群。

关键幂等规则：`run_id = sha256(文件内容)`，`feedback_id` 作为明细唯一键，重复上传只更新未处理记录，不重复创建 Issue。

## 用户流程

1. 用户把导出的反馈文件上传到指定目录或飞书表单。
2. n8n 触发工作流，调用 Python 清洗：去空、去重、脱敏、统一时间和 SKU。
3. LLM 按厨具问题分类体系输出结构化结果；低置信度进入人工复核表。
4. Python 聚合 SKU、渠道、日期、问题类别和严重程度，计算趋势。
5. Agent 生成“问题摘要 + 原文证据 + 建议动作 + 推荐负责人”。
6. n8n 写入飞书多维表，按规则创建任务并通知负责人。
7. 负责人更新任务状态；Agent 每周读取处理结果，比较问题量和重复投诉率，生成复盘报告。

## 首版分类体系

控制在 10 类以内，后续根据人工复核数据迭代：

1. 产品质量：涂层、开裂、变形、漏水、异味
2. 尺寸与规格
3. 功能/兼容性：电磁炉、燃气灶、容量、加热
4. 包装与物流损坏
5. 配件/赠品缺失
6. 使用方法与清洁
7. 商品描述或直播承诺不一致
8. 价格、优惠和活动规则
9. 售后服务与退款
10. 其他/待人工确认

## LLM 输出契约

每条反馈固定输出 JSON，禁止直接输出散文：

```json
{
  "category": "产品质量/涂层",
  "subcategory": "使用两个月后粘锅",
  "sentiment": "负面",
  "severity": "高",
  "is_actionable": true,
  "sku": "双枪-炒锅-28cm",
  "evidence": "使用两个月后开始粘锅",
  "suggested_owner": "品控",
  "suggested_action": "核查同批次涂层投诉，并抽检库存",
  "confidence": 0.91,
  "needs_human_review": false
}
```

必须加入：分类定义、反例、未知类别、置信度阈值和敏感信息禁止回显规则。`confidence < 0.75`、涉及赔付/安全事故或模型输出字段缺失时，自动进入人工复核。

## 飞书多维表设计

### Feedback 明细表

`feedback_id`、原文、脱敏原文、SKU、渠道、时间、分类、子类、情绪、严重程度、证据、置信度、是否需复核、批次号、来源文件。

### Issue 问题表

`issue_id`、问题标题、关联 SKU、问题类别、反馈数量、环比变化、典型证据、建议动作、负责人、截止日期、状态、复盘结论。

### Taxonomy 规则表

问题分类、定义、反例、关键词、版本、生效时间、维护人。

### Run Log 运行日志表

运行批次、开始/结束时间、输入条数、成功条数、失败条数、人工复核数、Token 成本、错误信息、工作流版本。

## 工作流节点

```text
上传文件/飞书按钮
  → 文件校验与脱敏
  → Python 清洗去重
  → 分批 LLM 分类
  → JSON Schema 校验与失败重试
  → 低置信度人工复核
  → 聚合趋势与 Top 问题
  → 生成建议和负责人
  → 写入飞书多维表
  → 飞书通知
  → 周期性复盘
```

## 2～3 周实施节奏

### 第 1 周：可用链路

- 准备 300～500 条脱敏样本和分类表。
- 完成 Python 清洗、去重、统计脚本。
- 完成结构化 Prompt、JSON Schema 和 50 条人工标注基准集。
- 在 Dify/Coze 中验证分类和摘要效果。

### 第 2 周：自动化与飞书协同

- 用 n8n 串起文件触发、Python、LLM 和飞书节点。
- 建立 Feedback、Issue、Taxonomy、Run Log 四张表。
- 加入失败重试、低置信度复核和运行日志。
- 完成负责人通知和任务状态回写。

### 第 3 周：复盘与演示

- 加入周报和趋势对比。
- 用 1000 条混合样本做压力测试。
- 补齐 SOP、使用手册、架构图和 3 分钟录屏。
- 准备一套“反馈暴增 → 定位 SKU → 分派处理 → 下周下降”的演示数据。

## 验收标准

- 单批 1000 条反馈，端到端运行成功率 ≥ 95%。
- 结构化字段完整率 ≥ 98%。
- 分类准确率以 100 条人工标注集计，目标 ≥ 85%。
- 低置信度和敏感问题拦截率 100%。
- 结果可追溯到原始反馈和运行批次。
- 从文件上传到飞书问题表生成不超过 10 分钟（不含人工复核）。
- 业务人员无需修改 Prompt，只需上传文件、审核和更新状态。

## 后续扩展顺序

1. 接入客服系统和平台评论 API。
2. 接入订单/物流查询，自动补全问题上下文。
3. 增加直播转写文本，分析“直播承诺—用户投诉”偏差。
4. 对高频问题自动生成商品详情页和直播话术修改草案。
5. 最后再考虑自动执行营销或售后动作。

## 已确认边界记录

1. 第一版使用 Excel/CSV 导入。
2. 可以创建飞书应用并调用多维表读写、消息推送 API。
3. 首要目标是面试作品 Demo。
